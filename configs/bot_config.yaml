app:
  name: Bot
  host: 0.0.0.0
  port: 8000
  prefix: /api/v1
  swagger_path: ./data/docs/server.json

  logger:
    level: TRACE

  telegram:
    token: TELEGRAM_TOKEN

  core_service:
    host: CORE_HOST
    port: CORE_PORT