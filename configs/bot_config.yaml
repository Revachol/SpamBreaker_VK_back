app:
  name: TGBot
  host: 0.0.0.0
  port: 8080
  prefix: /api/v1
  swagger_path: ./data/docs/server.json

  logger:
    level: TRACE

  telegram:
    token: TELEGRAM_TOKEN

  core_service:
    host: CORE_HOST
    port: CORE_PORT